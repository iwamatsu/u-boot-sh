if TARGET_LAGER

config SYS_BOARD
	default "lager"

config SYS_VENDOR
	default "renesas"

config SYS_CONFIG_NAME
	default "lager"

config SYS_TEXT_BASE
	default 0xB0000000 if RMOBILE_EXTRAM_BOOT
	default 0xE8080000

endif
