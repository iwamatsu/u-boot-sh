if TARGET_KOELSCH

config SYS_BOARD
	default "koelsch"

config SYS_VENDOR
	default "renesas"

config SYS_CONFIG_NAME
	default "koelsch"

config SYS_TEXT_BASE
	default 0x70000000 if RMOBILE_EXTRAM_BOOT
	default 0xE6304000

endif
